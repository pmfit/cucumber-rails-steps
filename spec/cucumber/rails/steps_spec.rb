# frozen_string_literal: true

RSpec.describe Cucumber::Rails::Steps do
  it "has a version number" do
    expect(Cucumber::Rails::Steps::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
